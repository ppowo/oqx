# oqx

`oqx` is a **CLI-first SOAP workbench**.

The project is currently scoped around a text-first workflow built on:

- pinned WSDL imports
- normalized contract indexing
- persistent request files
- `$EDITOR`-based XML editing
- render/send flows with environment and auth injection
- redacted history on disk

## Docs

- [`ARCHITECTURE.md`](./ARCHITECTURE.md) — architecture, storage model, contracts, environments, auth, history, and guardrails
- [`CLI_FLOW.md`](./CLI_FLOW.md) — command flow, end-to-end usage, WSDL update behavior, and v1 scope

## Project status

This repository currently captures the initial CLI-first foundation: architecture, command flows, and product scope for the first implementation.
